name = "lucky-store-neon-proxy"
main = "src/index.ts"
compatibility_date = "2024-09-23"
account_id = "8e457654e12c3b75d2094bbd8914030b"

workers_dev = true

[vars]
ALLOWED_ORIGINS = "https://adminweb-blond.vercel.app,https://lucky-store.vercel.app,http://localhost:5173,http://localhost:3000"

# Secrets (set via `wrangler secret put`):
# NEON_DATABASE_URL — Neon postgres connection string
# API_KEY — shared secret for admin_web to authenticate requests