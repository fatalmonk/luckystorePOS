# Lucky Store mobile public build configuration
#
# Reference only: pass these public values with Flutter --dart-define.
# This file is not bundled or loaded by the application.
#
# Never add staff credentials, payment credentials, service-role keys,
# database passwords, or other secrets to a mobile environment file.

SUPABASE_URL=<placeholder>
SUPABASE_ANON_KEY=<placeholder>
