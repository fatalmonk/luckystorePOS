{
  "buildCommand": "mkdir -p dist_public && cp -r landing/* dist_public/ && cd apps/admin_web && npm install && npm run build && mkdir -p ../../dist_public/admin && cp -r dist/* ../../dist_public/admin/",
  "outputDirectory": "dist_public",
  "rewrites": [
    { "source": "/privacy", "destination": "/privacy-policy.html" },
    { "source": "/terms", "destination": "/terms-of-service.html" },
    { "source": "/admin/(.*)", "destination": "/admin/index.html" },
    { "source": "/admin", "destination": "/admin/index.html" }
  ]
}
